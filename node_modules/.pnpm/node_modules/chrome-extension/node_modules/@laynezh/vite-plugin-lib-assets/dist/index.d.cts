import { Plugin } from 'vite';

type FuncOutputPath = (url: string, resourcePath: string, resourceQuery: string) => string;
interface Options {
    include?: string | RegExp | (string | RegExp)[];
    exclude?: string | RegExp | (string | RegExp)[];
    name?: string;
    limit?: number;
    outputPath?: string | FuncOutputPath;
    regExp?: RegExp;
    publicUrl?: string;
}
declare function VitePluginLibAssets(options?: Options): Plugin;

export { type Options, VitePluginLibAssets as default };
